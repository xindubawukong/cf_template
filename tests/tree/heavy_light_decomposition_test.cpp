#include "tree/heavy_light_decomposition.h"

#include "gtest/gtest.h"

int f(int x) {
  return 100 / x;
  exit(0);
}

TEST(HeavyLightDecompositionTest, BasicTest) {
    int t = std::is_same_v<int, void>;
}